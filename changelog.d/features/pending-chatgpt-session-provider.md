- Added `chatgpt-session` (alias `cgpt-session`), a clean-room ChatGPT Web provider serving
  `/v1/chat/completions` from an authenticated browser session. The restored `chatgpt-web`
  provider remains available separately; only its former `cgpt-web` alias is retired.
