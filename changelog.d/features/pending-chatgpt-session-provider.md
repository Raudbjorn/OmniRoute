- Added `chatgpt-session` (alias `cgpt-session`), a clean-room ChatGPT Web provider serving
  `/v1/chat/completions` from an authenticated browser session. `chatgpt-web` itself was restored
  by #12239 (clean-room browser transport); only its old `cgpt-web` alias remains retired.
